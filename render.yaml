services:
  - type: web
    name: blue-website
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: COMMANDS_TODAY_DEMO
        value: "0"
      - key: DISCORD_TOKEN
        sync: false
